import { StyleSheet, TextInput } from "react-native";

const styles = StyleSheet.create({
  initContainer: {
    padding: 10,
    marginVertical: 5,
    backgroundColor: "#eee",
    height: "100%",
  },
  textInput: {
    height: 50,
    backgroundColor: "#ffffff",
    marginVertical: 5,
    padding: 5,
  },
  row: { alignContent: "space-between", flexDirection: "row", padding: 10 },
  iconContainer: { width: 30 },
});

export default styles;
